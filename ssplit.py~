# to split any string we have split(). it takes a string as an argument. depending on that it will split the main string and returns a list containing solited strings.

s = "we all love python"
s = s.split(" ")
print(s)
